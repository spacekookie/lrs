dist: trusty
language: rust
services: docker
sudo: required
cache: cargo
rust:
  - stable
  - beta
  - nightly
matrix:
  allow_failures:
    - rust: nightly

script:
  - cargo build --verbose --all
  - cargo test --verbose --all

notifications:
  email:
    on_success: never
    on_failure: never